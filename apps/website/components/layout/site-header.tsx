"use client";
import Link from "next/link";
import { Menu, X, ArrowUpRight } from "lucide-react";
import { useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { nav } from "@/lib/site";
export function SiteHeader(){ const [open,setOpen]=useState(false); return <header className="sticky top-0 z-50 border-b border-[var(--line)] bg-[color:var(--paper)]/90 backdrop-blur-xl"><div className="container flex h-18 items-center justify-between gap-6"><Link href="/" className="flex items-center gap-2 font-black tracking-[-.06em] text-xl"><span className="grid h-8 w-8 place-items-center rounded-full bg-[var(--ink)] text-[var(--lime)]">D</span>DRAGON<span className="font-normal text-[var(--muted)]">/STUDIOS</span></Link><nav className="hidden items-center gap-6 lg:flex">{nav.map(x=><Link className="eyebrow transition hover:text-[var(--orange)]" href={x.href} key={x.href}>{x.label}</Link>)}</nav><div className="hidden items-center gap-3 lg:flex"><Link href="/login" className="eyebrow">Sign in</Link><Link href="/downloads" className="flex items-center gap-1 rounded-full bg-[var(--ink)] px-4 py-2 text-xs font-bold text-white">Get the launcher <ArrowUpRight size={14}/></Link></div><button aria-label="Open menu" className="lg:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-[var(--line)] lg:hidden"><nav className="container flex flex-col py-5">{nav.map(x=><Link onClick={()=>setOpen(false)} className="display border-b border-[var(--line)] py-3 text-4xl" href={x.href} key={x.href}>{x.label}</Link>)}<Link className="mt-5 rounded-full bg-[var(--ink)] px-4 py-3 text-center text-sm font-bold text-white" href="/login">Sign in</Link></nav></motion.div>}</AnimatePresence></header> }
